//
//  elegirIngredientes.swift
//  Pizza 2
//
//  Created by Instituto Cultural Cuernavaca on 07/01/16.
//  Copyright © 2016 Instituto Cultural Cuernavaca. All rights reserved.
//

import UIKit

class elegirIngredientes: UIViewController {

    @IBOutlet weak var eleccionIngredientes: UILabel!
    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    @IBAction func enviarPizza(sender: AnyObject) {
    }
    @IBAction func elegirAceitunas(sender: AnyObject) {
    }
    @IBAction func elegirAnchoas(sender: AnyObject) {
    }
    @IBAction func elegirPimiento(sender: AnyObject) {
    }
    @IBAction func elegirPiña(sender: AnyObject) {
    }
    @IBAction func elegirPepperoni(sender: AnyObject) {
    }

    @IBAction func elegirJamon(sender: AnyObject) {
    }
    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepareForSegue(segue: UIStoryboardSegue, sender: AnyObject?) {
        // Get the new view controller using segue.destinationViewController.
        // Pass the selected object to the new view controller.
    }
    */

}
